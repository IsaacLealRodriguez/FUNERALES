name: funeraria_gestion
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:

  open_street_map_search_and_pick: 0.1.1
  url_launcher: ^6.3.0
  flutter:
    sdk: flutter

  # Iconos estilo iOS
  cupertino_icons: ^1.0.8
  
  # Base de datos
  supabase_flutter: ^2.12.0
  
  # Formato de fechas y números
  intl: ^0.20.2

  # --- NUEVAS LIBRERÍAS PARA PDF ---
  pdf: ^3.10.0
  printing: ^5.11.0

# --- AGREGA ESTO PARA LAS LLAMADAS ---

intl: ^0.19.0


url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/blanco.png
    - assets/negro.png